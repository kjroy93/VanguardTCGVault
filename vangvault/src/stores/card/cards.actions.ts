/**
 * ARCHIVO LEGADO SIN USO
 * ----------------------
 * Las acciones reales de cartas están dentro de `card.store.ts`.
 *
 * Este archivo vacío procede de una estructura anterior y no interviene en
 * los fetch, el filtrado, el estado ni el renderizado.
 */
