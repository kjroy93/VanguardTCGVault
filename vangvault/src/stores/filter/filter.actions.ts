/**
 * ARCHIVO LEGADO SIN USO
 * ----------------------
 * Las dos acciones del store de filtros (`setFilter` y `resetFilters`) están
 * definidas directamente en `filter.store.ts`, como permite Pinia.
 */
